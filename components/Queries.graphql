query getPasswordHash {
    signIn(username: "", password: ""){
        ...on SignInSuccess{
            passwordHash
        }

        ...on FailurePayload{
            exceptionName
            errorMessage
        }
    }
}